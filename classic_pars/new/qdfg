SELECT load_ts_xml, COUNT(DISTINCT transactionsequencenumber) as txn_count
FROM ice_baby.core_flow_ing_raw.raw_bptransaction
GROUP BY load_ts_xml
HAVING COUNT(DISTINCT transactionsequencenumber) > 1
LIMIT 10;
