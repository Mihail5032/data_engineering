SELECT MIN(load_ts_xml) as first_record,
       MAX(load_ts_xml) as last_record,
       COUNT(*) as total,
       COUNT(DISTINCT load_ts_xml) as distinct_timestamps
FROM ice_baby.core_flow_ing_raw.raw_bptender;
