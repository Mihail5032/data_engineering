
SELECT tendertypecode, COUNT(*) as cnt
FROM raw_bptender
WHERE businessdaydate >= '2026-03-28'
GROUP BY retailstoreid, businessdaydate, workstationid, transactionsequencenumber, tendertypecode, tenderamount
HAVING COUNT(*) > 1
ORDER BY cnt DESC
LIMIT 20;
