# Переключись на dev
git checkout dev

# Скопируй папку из ветки new_parser
git checkout feature/new_parser -- xml-parser-correction/

# Закоммить
git add xml-parser-correction/
git commit -m "add xml-parser-correction from new_parser"

# Запушь
git push origin dev
